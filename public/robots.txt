# Crawling is open. The embed iframe variants (?embed=…) are kept out of
# the index by a page-level noindex meta (injected in index.html), which
# crawlers can only see because crawling is allowed here — do not add a
# Disallow rule, or the noindex becomes invisible and the embeds can
# index anyway.
User-agent: *
Allow: /
