# Allow everything — indexing control lives in the page itself:
# the clean URL is indexable with a self-canonical, and embed/param
# variants carry an injected noindex robots meta. Blocking them here
# would hide that noindex from crawlers, so don't.
User-agent: *
Allow: /
