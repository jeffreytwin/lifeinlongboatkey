# PRE-LAUNCH LOCKOUT — remove before submitting to search engines.
# Blocks all crawling of the map while the site is in final checks.
#
# At launch, replace this file's rules with the open version:
#   User-agent: *
#   Allow: /
# (Crawling must be allowed at launch so crawlers can see the page-level
# noindex on the embed variants — robots.txt blocking would hide it.)
User-agent: *
Disallow: /
