/**
 * Details panel — stub for MVP. The mockup shows community details inside
 * the map popup; Parrish has a full side panel. For this MVP we keep the
 * Mapbox popup as the primary detail surface (see map.js). This module
 * exists so the next plan (mobile drawer + photos) can drop a real panel
 * in without restructuring callers.
 */

export function initDetailsPanel() {
  // no-op for MVP
}

export function showCommunityDetails(_community) {
  // no-op for MVP — map popup handles it
}

export function closeDetailsPanel() {
  // no-op for MVP
}
