// config.example.js - Template for configuration settings
// Copy this file to config.js and add your own values

// Application configuration
window.config = {
    // Replace with your actual Mapbox access token (from mapbox.com)
    mapboxAccessToken: 'YOUR_MAPBOX_ACCESS_TOKEN_HERE',

    // Other configuration options
    appName: 'Life in Longboat Key',
    appVersion: '1.0.0',
};
